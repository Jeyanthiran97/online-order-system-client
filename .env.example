# API Configuration
NEXT_PUBLIC_API_URL=

# Cloudinary Configuration
# These are required for client-side image uploads
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=

# Note: Make sure you've created the upload preset in Cloudinary dashboard
# Go to: https://cloudinary.com/console → Settings → Upload → Upload presets
# Create a preset named "online-shop-uploads" (or update the name above to match)
# Set signing mode to "Unsigned" for client-side uploads
